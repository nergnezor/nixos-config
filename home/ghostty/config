# Minimal default config, written after the original (and its backup and
# theme exports) turned out corrupted with no clean copy recoverable
# anywhere -- see nixos-config/PARTITION-RUNBOOK.md for the full story.
# Build this back up as you remember/want specific settings.

theme = noctalia
background-opacity = 0.7
font-size = 11
cursor-style = block
mouse-hide-while-typing = true
confirm-close-surface = false
