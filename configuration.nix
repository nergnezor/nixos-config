{ pkgs, lib, ... }:
let
  # How much bigger everything gets in the RDP/Plasma session. An RDP client
  # on a phone hands over the handset's own resolution, which is physically
  # tiny, so unscaled Plasma renders at phone-pixel size and is unusable by
  # touch. Bump/lower this one number to retune it. niri's scaling is
  # per-output in niri/noctalia/outputs.kdl and is unaffected -- these
  # variables are exported inside the xrdp session wrapper below, so they
  # only ever exist in the Xorg xrdp spins up per connection; greetd/niri
  # never sources them.
  rdpScale = 2.5;
  # GTK's scale is integer-only, so it takes the next whole step up and
  # GDK_DPI_SCALE divides text back down to the same effective factor. Qt
  # (Plasma itself and every KDE app) takes the fraction directly.
  gdkScale = builtins.ceil rdpScale;
in
{
  # Generated by `nixos-generate-config --root /mnt` during install — copy
  # the result here (see PARTITION-RUNBOOK.md step 9) before `nixos-install`.
  imports = [ ./hardware-configuration.nix ];

  nixpkgs.config.allowUnfree = true; # vivaldi, vscode, claude-code
  # noctalia-shell.overlays.default was tried and reverted: an overlay
  # builds against THIS nixpkgs, and the meson build wanted a
  # wayland-protocols staging file this pin didn't have.
  # noctalia-shell.nixosModules.default (imported in flake.nix) already
  # sets programs.noctalia.package to the flake's own package output.
  #
  # Ubuntu started noctalia via ~/.local/share/systemd/user/noctalia.service
  # (ExecStart=/usr/local/bin/noctalia). That path is Ubuntu-only, so the
  # unit fails here; putting the binary in home.packages does not help,
  # since nothing execs that store path. This NixOS unit lives in
  # /etc/systemd/user, which outranks ~/.local/share, so the same unit
  # *name* (and the enable symlink under graphical-session.target.wants)
  # runs the store path instead.
  #
  # **That precedence only holds against ~/.local/share.**
  # systemd's user unit search order is, highest first:
  #   1. ~/.config/systemd/user      2. /etc/systemd/user
  #   3. ~/.local/share/systemd/user 4. /usr/lib/systemd/user
  # so anything in ~/.config/systemd/user SHADOWS what this module installs.
  # Ubuntu-era units restored into that directory are therefore able to
  # break the session outright — a unit pointing at a binary that does not
  # exist on NixOS, or a file the rescue brought back corrupt. If a session
  # dies the instant it opens, look there first.
  #
  # Do NOT use the home-manager module for this: it writes the unit into
  # ~/.config/systemd/user, which is both the shadowing path above and a
  # collision with whatever already sits there — the same class of failure
  # as the mouseless.service abort.
  programs.noctalia = {
    enable = true;
    systemd.enable = true;
    recommendedServices.enable = true; # bluetooth, upower, power-profiles-daemon
  };

  boot.loader.systemd-boot.enable = true;
  boot.loader.efi.canTouchEfiVariables = true;
  # The ESP is 512MiB and each generation puts a kernel plus an initrd on it,
  # roughly 100MiB a time. Without a cap `nixos-rebuild` eventually fills it
  # and fails partway through a switch, which is a bad moment to discover a
  # full /boot. Five is a couple of months of rebuilds and still leaves
  # plenty to roll back to; older generations stay in the store and reappear
  # here after a `nix-collect-garbage` and a rebuild if you need them.
  boot.loader.systemd-boot.configurationLimit = 5;
  # There is only one OS to boot now, so this is not a menu for choosing
  # between systems — it is the one for choosing an older generation when a
  # rebuild breaks something. Short, but never zero.
  boot.loader.timeout = 3;
  boot.loader.efi.efiSysMountPoint = "/boot"; # nvme0n1p2 (label NIXBOOT) on
    # the internal disk; also matches disko-usb.nix's ESP mountpoint.
  # This ESP was created for NixOS alone and never held Ubuntu's GRUB. The
  # hp-envy is now NixOS-only, so there is no boot menu to choose between
  # them any more; nitro is still dual-boot (see hosts/nitro.nix).

  # Per-host: networking.hostName, GPU driver, and how /home is provided.
  # hp-envy has a dedicated home partition; nitro still shares Ubuntu's
  # (see hosts/nitro.nix). Both use `nofail`, so a config accidentally built
  # for the wrong host degrades to a normal empty local home instead of a
  # broken boot.

  networking.networkmanager.enable = true;

  # Debugging this machine from its own console has been the hard part of
  # every problem it has had: a garbled framebuffer or a wrong console font
  # makes journalctl unreadable exactly when you most need it. sshd gives a
  # way in that does not go through the display at all. Password auth stays
  # off -- ~/.ssh/authorized_keys on the restored home is the way in, and
  # `ssh-copy-id` from a trusted machine is how it gets populated.
  services.openssh = {
    enable = true;
    settings = {
      PasswordAuthentication = false;
      PermitRootLogin = "no";
    };
  };

  # Lets the phone reach this machine over SSH without port-forwarding or
  # DDNS on the router: tailscaled joins it to a private WireGuard mesh with
  # the other devices on the same Tailscale account, so it's reachable at a
  # stable Tailscale IP/MagicDNS name from anywhere, NAT/CGNAT included.
  # `sudo tailscale up` after the rebuild does the one-time interactive
  # login. SSH itself stays as configured above (key-only) -- Tailscale is
  # just the network path in, not a replacement for that auth.
  services.tailscale.enable = true;

  # A separate, log-in-able remote desktop -- NOT a mirror of the running
  # niri session. niri is Wayland-only and xrdp's xorgxrdp backend only
  # ever drives its own private Xorg, spun up fresh per RDP connection, so
  # the two can't be the same desktop; Plasma (X11 session) is what runs
  # inside that private X session. (Mirroring the actual niri screen
  # instead would be wayvnc, not xrdp -- different tool, not configured
  # here.)
  #
  # desktopManager.plasma6.enable, not services.xserver.enable: this only
  # registers the Plasma session package, it does not turn Plasma (or any
  # X server) into the local display manager -- greetd/niri keeps that.
  # Chosen over Xfce for RDP use because Plasma 6 auto-switches to a
  # touch-friendly layout (larger hit targets, "Tablet Mode") when it
  # detects a touchscreen -- relevant since this session gets used from a
  # phone RDP client, not just a desktop one.
  services.desktopManager.plasma6.enable = true;
  services.xrdp = {
    enable = true;
    # A wrapper rather than startplasma-x11 straight, purely so the scaling
    # environment above is set for the session and nothing else on the
    # machine. Xft.dpi is deliberately left alone: GTK reads it too, and
    # combined with GDK_SCALE it would scale text twice.
    defaultWindowManager = "${pkgs.writeShellScript "xrdp-plasma-session" ''
      export QT_SCALE_FACTOR=${toString rdpScale}
      export GDK_SCALE=${toString gdkScale}
      export GDK_DPI_SCALE=${toString (rdpScale / gdkScale)}
      export XCURSOR_SIZE=${toString (builtins.floor (24 * rdpScale))}
      exec ${pkgs.kdePackages.plasma-workspace}/bin/startplasma-x11
    ''}";
  };
  # plasma6.nix and niri.nix both set services.displayManager.defaultSession
  # at the same priority, which is an eval error. Neither module's setting
  # is actually consulted, though: greetd execs niri-session directly (see
  # its settings.default_session.command below) rather than reading this
  # option, and xrdp gets its session from defaultWindowManager above. Force
  # it to niri just to break the tie.
  services.displayManager.defaultSession = lib.mkForce "niri";
  # Port 3389 is only reachable over the Tailscale mesh, never the LAN/WAN
  # -- RDP is a common scanning target and password auth is off everywhere
  # else on this machine for the same reason. tailscale0 exists thanks to
  # services.tailscale.enable above.
  networking.firewall.interfaces.tailscale0.allowedTCPPorts = [ 3389 ];

  time.timeZone = "Europe/Stockholm"; # adjust if wrong
  i18n.defaultLocale = "en_US.UTF-8";

  # The keyboard is Swedish; without these NixOS falls back to the US
  # layout everywhere. That is not cosmetic: the root/erik passwords were
  # typed on a Swedish layout during `nixos-install` (run from the Ubuntu
  # session, see PARTITION-RUNBOOK.md), so every non-alphanumeric character
  # in them lands on a different key at the greetd/tuigreet prompt and the
  # login simply fails, with no hint as to why.
  #
  # console.keyMap covers the VTs and tuigreet. useXkbConfig is deliberately
  # NOT used to derive it -- that would make the console depend on the X11
  # settings, and this machine has no X server.
  console.keyMap = "sv-latin1";
  # niri's own xkb block (niri/config.kdl) is empty, which per its docs
  # means it reads the layout from org.freedesktop.locale1 -- which is what
  # services.xserver.xkb.layout populates, X server or not. So this is what
  # gives the Wayland session a Swedish layout too.
  services.xserver.xkb.layout = "se";

  # tuigreet draws its frame with Unicode box-drawing glyphs, which the
  # kernel's built-in 8x16 console font does not have.
  #
  # Name the kbd font, do NOT point at a .psf.gz in the store, and do NOT
  # turn on earlySetup. Tried that first and it made things worse rather
  # than better: most characters rendered as some *other* character while a
  # few stayed correct, which is the signature of text being indexed into
  # the font byte-by-byte because the font's unicode table never got
  # applied. A bare name goes through the normal (post-initrd) console
  # setup, which loads the map with it.
  console.font = "Lat2-Terminus16";

  # uid AND gid both have to match Ubuntu's erik (1000:1000), not just the
  # uid: the home directory is shared between the two systems, so files
  # created from NixOS would otherwise land with group 100 ("users", the
  # NixOS default) against Ubuntu's 1000. The missing `erik` group also
  # broke systemd-tmpfiles on the first boot — the /dev/uinput rules below
  # name it: "Failed to resolve group 'erik': No such process".
  users.groups.erik.gid = 1000;
  users.users.erik = {
    isNormalUser = true;
    uid = 1000;
    group = "erik";
    extraGroups = [ "wheel" "networkmanager" "video" "input" ];
    shell = pkgs.bash;
  };

  hardware.graphics.enable = true; # GPU-specific extras (nvidia, intel-media-driver) are per-host

  # nixpkgs' own programs.niri.enable (not niri-flake). niri-flake forced a
  # from-source Rust build that hit a crates.io 403; this pin ships niri
  # 26.04 as a cached binary AND, via systemd.packages, installs its user
  # units (niri.service, niri-shutdown.target). Without those units,
  # niri-session cannot start niri.service, graphical-session.target never
  # becomes active, and every WantedBy=graphical-session.target user unit
  # in the shared home stays dead — including Ubuntu's noctalia.service
  # and mouseless.service. greetd must exec niri-session, not the bare
  # niri binary, for the same reason.
  programs.niri = {
    enable = true;
    useNautilus = false; # gtk file-chooser is enough; nautilus is a big extra
  };

  services.greetd = {
    enable = true;
    settings.default_session = {
      # pkgs.tuigreet, not pkgs.greetd.tuigreet -- it moved out of the greetd
      # attribute set into a top-level package in newer nixpkgs.
      #
      # --sessions instead of --cmd, deliberately. --cmd hardcodes one
      # command as the default session, but tuigreet ALSO scans for
      # wayland-sessions/*.desktop and lists what it finds, so niri showed
      # up twice in the F3 session menu -- once as the raw store path from
      # --cmd, once as "Niri" from niri.desktop -- and the two are not
      # equivalent: the --cmd form runs the binary with none of the
      # desktop entry's DesktopNames=niri, so XDG_CURRENT_DESKTOP is unset
      # in that session and the portal/xdg bits that key off it misbehave.
      # Pointing --sessions at the NixOS session directory leaves exactly
      # one niri entry, the desktop-file one, which is the correct one.
      #
      # The scan is not optional: without --sessions tuigreet falls back to
      # $XDG_DATA_DIRS, and greetd.service has a bare environment (PATH,
      # LOCALE_ARCHIVE, TZDIR only) so that is unset and it would look in
      # /usr/local/share:/usr/share -- neither of which exists on NixOS.
      #
      # No --xsessions: /run/current-system/sw/share/xsessions does not
      # exist on this machine (no X server; Plasma is registered only for
      # xrdp, which gets its session from xrdp.defaultWindowManager).
      #
      # --remember-session keeps the last choice preselected, which is what
      # --cmd was really providing.
      command = "${pkgs.tuigreet}/bin/tuigreet --time --remember --remember-session --sessions /run/current-system/sw/share/wayland-sessions";
      user = "greeter";
    };
  };

  security.polkit.enable = true;
  services.pipewire = {
    enable = true;
    alsa.enable = true;
    alsa.support32Bit = true; # steam
    pulse.enable = true;
  };
  services.pulseaudio.enable = false; # was hardware.pulseaudio, renamed on this nixpkgs revision

  # mouseless (net.sonuscape.mouseless) used to come from the Ubuntu side's
  # flatpak install, which is gone with Ubuntu. It is NOT on flathub -- it
  # ships from Sonuscape's own repo, so that remote has to exist first or
  # the install simply does not resolve:
  #   flatpak remote-add --if-not-exists sonuscape https://dl.sonuscape.net/flatpak/repo
  #   flatpak install sonuscape net.sonuscape.mouseless
  #
  # Both are `--user` scope on this machine (confirmed with
  # `flatpak remotes -d`), and they stay as commands rather than config
  # because nixpkgs has no option for declaring flatpak remotes at all --
  # that needs the nix-flatpak flake, which is not an input here.
  services.flatpak.enable = true;
  xdg.portal = {
    enable = true; # required assertion for flatpak; niri itself also needs a portal for screen-share/file-pickers
    extraPortals = [ pkgs.xdg-desktop-portal-gtk ]; # generic backend, not niri-specific — good enough for a smoke test
  };

  programs.steam.enable = true;
  # gamescopeSession dropped along with the rest of the trimmed app set --
  # erik only asked for plain steam back, not gamescope.

  # niri ships no Xwayland of its own, so in a bare niri session DISPLAY is
  # unset and every X11-only app dies -- steam pops "Unable to open a
  # connection to X". xwayland-satellite supplies the X server. The display
  # number is pinned to :0 so niri's environment block (shared home,
  # ~/.config/niri/config.kdl) can hand the same DISPLAY to the clients niri
  # spawns; those inherit niri's env and would otherwise miss the DISPLAY
  # that xwayland-satellite pushes into the systemd/dbus activation env.
  systemd.user.services.xwayland-satellite = {
    description = "Xwayland outside your Wayland";
    bindsTo = [ "graphical-session.target" ];
    partOf = [ "graphical-session.target" ];
    after = [ "graphical-session.target" ];
    requisite = [ "graphical-session.target" ];
    wantedBy = [ "graphical-session.target" ];
    serviceConfig = {
      Type = "notify";
      NotifyAccess = "all";
      ExecStart = "${pkgs.xwayland-satellite}/bin/xwayland-satellite :0";
      StandardOutput = "journal";
    };
  };

  # nix-ld gör att NixOS kan köra vanliga dynamiskt länkade Linux-binärer
  programs.nix-ld.enable = true;

  # mouseless's own user unit. Ubuntu's copy of this sat in
  # ~/.config/systemd/user, was hand-restored after the rescue, and was
  # tracked nowhere -- so a fresh machine got the udev rules and tmpfiles
  # entries below, and the start script from this repo, but nothing that
  # ever started it.
  #
  # Declared here and NOT in home.nix on purpose: home-manager writes user
  # units into ~/.config/systemd/user, which is both the directory that
  # SHADOWS what this module installs (see the search-order note at the top
  # of this file) and the exact path Ubuntu's file already occupied -- the
  # collision that once aborted the whole activation and left the profile
  # with no packages. The NixOS module writes /etc/systemd/user instead.
  #
  # **Remove the handwritten ~/.config/systemd/user/mouseless.service and
  # its graphical-session.target.wants/ symlink** when adopting this, or the
  # old file keeps shadowing this one and nothing appears to change.
  #
  # ExecStart keeps Ubuntu's %h form rather than a store path: ~/.config/niri
  # is an mkOutOfStoreSymlink into this repo (home.nix), so %h resolves to
  # the working-tree script and editing it does not need a rebuild. ExecStop
  # does the opposite and uses absolute store paths -- a user unit gets a
  # near-empty PATH, and the original's bare `flatpak`/`pkill` (and its
  # /bin/bash, which is only a compat symlink here) are not something to
  # rely on at shutdown.
  systemd.user.services.mouseless = {
    description = "Mouseless keyboard mouse control";
    partOf = [ "graphical-session.target" ];
    wants = [ "graphical-session.target" ];
    after = [ "graphical-session.target" "niri.service" ];
    wantedBy = [ "graphical-session.target" ];
    serviceConfig = {
      Type = "exec";
      ExecStart = "%h/.config/niri/scripts/start-mouseless.sh";
      # Flatpak reparents into its own scope, so stopping this unit
      # otherwise leaves the old python process -- and its virtual
      # mouse/joystick device -- running.
      ExecStop = "${pkgs.bash}/bin/bash -c '${pkgs.flatpak}/bin/flatpak kill net.sonuscape.mouseless; ${pkgs.procps}/bin/pkill -f \"/app/share/mouseless/src/main.pyc\" || true'";
      TimeoutStopSec = 15;
      Restart = "on-failure";
      RestartSec = 10;
      Slice = "app.slice";
    };
  };

  # Mouseless virtual-mouse/device quirks, ported verbatim from this Ubuntu
  # install's /etc/udev/rules.d/61-mouseless-no-takeover.rules and
  # /etc/tmpfiles.d/mouseless-input.conf.
  services.udev.extraRules = ''
    ACTION=="add|change", SUBSYSTEM=="input", KERNEL=="event*", ATTRS{name}=="Mouseless-Virtual-Mouse", ENV{ID_INPUT_JOYSTICK}="0", ENV{ID_INPUT_MOUSE}="1"
    ACTION=="add|change", SUBSYSTEM=="input", KERNEL=="js*", ATTRS{name}=="Mouseless-Virtual-Mouse", MODE="0000", GROUP="root"
    ACTION=="add|change", SUBSYSTEM=="input", KERNEL=="event*", ATTRS{name}=="*Controller Touchpad*", ENV{LIBINPUT_IGNORE_DEVICE}="1", ENV{ID_INPUT_MOUSE}="0", ENV{ID_INPUT_TOUCHPAD}="0"
  '';
  systemd.tmpfiles.rules = [
    "z /dev/uinput       0660 erik erik - -"
    "z /dev/input/event* 0660 erik erik - -"
  ];

  nix.settings.experimental-features = [ "nix-command" "flakes" ];

  system.stateVersion = "25.05"; # matches the nixpkgs/home-manager release-25.05 pin
}
